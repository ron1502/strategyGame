class Medium(Project):
    def __init__():
        super("medium.jpg", "Medium", 100, 99, 99, 99, 99, 99, 99, 99)
        xpos = super.getX()
        ypos = super.getY()

    def update(Graphics g):
        xpos = super.getX()
        ypos = super.getY()
        # Movement Function
        super.update(g)